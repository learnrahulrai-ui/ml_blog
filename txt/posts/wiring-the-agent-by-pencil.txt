@title The World Calls Three Times: Wiring the Agent by Pencil :: Rahul's ML Blog
==============================================================================================
  RAHUL'S ML BLOG -- notes on machine learning, worked out by hand                    est. 2026
==============================================================================================
  [home](../index.txt) | [about](../about.txt) | [archive](../archive.txt) | [glossary](../glossary.txt) | [contact](mailto:learn.rahul.rai@gmail.com)
----------------------------------------------------------------------------------------------

  CHAPTER 15 . TRAINING THE Q-NETWORK . PART 4 OF 4
  The World Calls Three Times: Wiring the Agent by Pencil
  Posted: 2026-07-01 . Author: Rahul Rai . Tags: reinforcement-learning, deep-rl, agent, experience-replay, terminal, by-hand
  ============================================================================================

  PATH . post 48/51 . part 4 of 4 in Chapter 15 -- Training the Q-Network
    <- prev:  [The Frozen Twin: A Batch of Misses at Once](batch-td-error-by-pencil.txt)
       next:  [Chapter 16, Propose and Grade: Actor-Critic by Pencil](actor-critic-by-pencil.txt) ->

  ---

  This chapter built the pieces that TRAIN a Q-network: a net that reads a spot and hands back a
  worth per move (Chapter 14); backprop, which walks one miss back to every dial (Part 1); Adam,
  which spends each dial's nudge well (Part 2); and the replay bin plus the frozen twin, which grade
  a whole batch of stored moves against a held-still copy of the net (Part 3). Four working parts --
  and still no agent. An agent is not a pile of functions; it is a thing the world can CALL, moment
  by moment, that answers with a move and quietly learns on the side. This post wires the parts into
  that callable thing, then runs it on the lander.

  A worth = the net's guess of total future reward for a move. A move = which engine to fire. A spot
  (a state) = the lander's eight dials right now. The replay bin = a list of past moves, each stored
  as five things (the spot left, the move taken, the reward, a terminal flag, the spot landed on).
  All four carried in unchanged from earlier in the chapter.

  ---

  SO THE WORLD DOES NOT RUN THE AGENT -- IT CALLS IT, AT THREE KINDS OF MOMENT

  You do not write a loop that drives the lander. The world (the simulator) runs the loop, and it
  reaches into your agent at three kinds of moment: the FIRST moment of a run, every MIDDLE moment,
  and the LAST moment. So the agent is three functions the world calls, not one:

      episode timeline (one run of the lander):

        first        middle        middle              last
        moment       moment        moment      ...     moment
          |            |             |                   |
          v            v             v                   v
      agent_start  agent_step    agent_step          agent_end
       (S0)         (R1, S1)      (R2, S2)            (R_last)

  At the first moment the world has handed over a starting spot but no reward yet (nothing has
  happened). At every middle moment it hands a reward and the new spot its last move produced. At the
  last moment the run is over -- it hands the final reward and there is no new spot. Three moments,
  three functions.

  ---

  WHICH MEANS THE FIRST CALL ONLY PICKS A MOVE AND REMEMBERS IT

  The first call, agent_start, gets the starting spot S0 and nothing else. There is no reward to
  learn from yet, so it does exactly two things: pick a move, and write down what it did.

      agent_start(S0):
        pick a move A0 from S0        (softmax over the net's four worths -- below)
        remember:  last_spot  = S0
                   last_move  = A0
        hand A0 back to the world

  Picking the move: read the net's four worths at S0, turn them into chances with softmax -- raise e
  to each worth, divide each by the total, so the four are positive and add to 1, a bigger worth
  earning a bigger slice -- then SAMPLE one engine from those chances (not always the biggest, so no
  engine goes untried). Call the sampled engine A0.

  The two "remember" lines are the whole subtlety of this post, so look hard at what is stored:

      last_spot = S0    (the spot we were just handed)
      last_move = A0    (the move we are about to hand back for the world to execute)

  last_move is not a move already carried out. It is the move the world is ABOUT to carry out next.
  Why that matters is the next section.

  ---

  BECAUSE THE NEXT CALL MUST STORE last_move AS THE CAUSE OF THE REWARD IT RECEIVES

  When the world calls agent_step, it hands back a reward R1 and a new spot S1. That reward and that
  spot are the RESULT of the move the agent last handed over -- A0. So the transition to drop in the
  bin is "from S0, move A0, got R1, landed S1." That is why last_move had to hold A0: it is the cause
  of the reward now arriving. Draw the first two middle calls in order:

      agent_start:  pick A0                     remember last_spot=S0, last_move=A0
                    (world executes A0)
      agent_step 1: world gives R1, S1
                    STORE (last_spot, last_move, R1, S1) = (S0, A0, R1, S1)   <- correct
                    pick A1                     remember last_spot=S1, last_move=A1
                    (world executes A1)
      agent_step 2: world gives R2, S2
                    STORE (last_spot, last_move, R2, S2) = (S1, A1, R2, S2)   <- correct

  Now see the bug that lurks if the "remember" lines are skipped. Suppose agent_step 1 stored its
  transition but forgot to update last_move to A1. Then at agent_step 2, last_move is still A0, and
  the stored transition would read:

      STORE (S1, A0, R2, S2)   <- WRONG: R2 came from A1, but A0 is blamed

  A reward would be pinned on a move that did not cause it, and the net would learn a lie. The two
  "remember" lines exist so every stored transition blames the RIGHT move. That is the entire reason
  last_move is saved on every call: it must always hold the move whose reward the next call will
  receive.

  ---

  SO A MIDDLE CALL DOES FOUR THINGS: STORE, LEARN, PICK, REMEMBER

  With the timing settled, the middle call is straightforward. agent_step gets (reward R, new spot
  S), and in order:

      agent_step(R, S):
        STORE   (last_spot, last_move, R, terminal=0, S)   -> drop in the replay bin
        LEARN   run the replay updates (below)
        PICK    a move A from S            (softmax over the net's worths at S, then sample)
        REMEMBER  last_spot = S ,  last_move = A
        hand A back to the world

  The terminal flag stored is 0, because the run did NOT end on this move -- there is a real next
  spot S to bootstrap from. LEARN is everything the chapter already built, restated in one breath:
  grab a random batch of past transitions from the bin; for the batch, read each landing's worth
  through the FROZEN twin and each left-spot's worth through the live net, subtract to get a
  how-wrong per transition (Part 3); walk each how-wrong back to every dial (Part 1); and take one
  Adam step on the batch's averaged nudges (Part 2). One real move in, several stored moves rehearsed
  out.

  ---

  BUT THE LAST CALL HAS NO NEXT SPOT, SO ITS TRANSITION IS MARKED TERMINAL

  When the run ends -- the lander touches down or crashes -- the world calls agent_end with only a
  final reward R. There is no new spot: the game is over. So agent_end stores a transition whose
  landing spot is a dead placeholder, MARKED with terminal = 1, and returns no move (there is nothing
  left to do):

      agent_end(R):
        STORE   (last_spot, last_move, R, terminal=1, zero_spot)   -> drop in the bin
        LEARN   run the replay updates (same as agent_step)
        (return nothing -- the run is over)

  That terminal = 1 is the flag Part 3 was built to read. When this transition is later replayed, its
  target is reward + dilute x (worth of the landing) x (1 - terminal) = R + dilute x (worth) x 0 = R
  -- the bare final reward, no faded future, because there IS no future past the end. A crash's target
  is its penalty alone; a landing's target is its bonus alone. The two middle-vs-end calls, side by
  side:

      what              agent_step (mid-run)             agent_end (run over)
      ----              --------------------             --------------------
      world hands       reward R, new spot S             reward R only
      terminal flag     0 (a real next spot follows)     1 (nothing follows)
      landing stored    S                                a dead placeholder spot
      returns           a move for the world to run      nothing (episode done)

  ---

  SO STRUNG TOGETHER, THE THREE CALLS ARE A LANDER THAT LEARNS TO FLY

  The world runs its own loop and knocks on the three doors; the agent answers and learns on the
  side. One whole run reads: agent_start picks the opening move; agent_step fires for every middle
  moment -- store, learn, pick, remember -- until the lander lands or crashes; agent_end stores the
  final terminal move and learns one last time. Then the world starts the next run and calls
  agent_start again. Nothing else drives it.

  Run that over a few hundred runs and the learning curve climbs: the early runs crash (the net's
  worths are near-random, the softmax nearly a coin-toss), but every stored move feeds the replay
  bin, every batch nudges the dials, and the worths sharpen until the softmax reliably fires the
  engine that keeps the lander upright and slows its fall. The pile of functions from Parts 1 to 3,
  wired through these three calls, is a complete deep-RL agent flying LunarLander.

  ---

  THE CODE -- HARD-CODED, EVERY STEP SHOWN

  Nothing above needed a computer. This section is for the day you meet one. The learn step calls the
  batch machine from Parts 1 to 3 (shown there); here the wiring is the point.

      # --- the first moment ---
      def agent_start(state):
          action = pick_move(state)            # softmax over net's worths, then sample
          self.last_spot = state               # remember the spot we were handed
          self.last_move = action              # remember the move about to be executed
          return action                        # hand it to the world

      # --- every middle moment ---
      def agent_step(reward, state):
          # store the transition whose reward just arrived: terminal 0 (a real next spot)
          self.buffer.append((self.last_spot, self.last_move, reward, 0, state))
          replay_learn()                       # grab a batch -> how-wrong (Part 3) -> backprop -> Adam
          action = pick_move(state)            # pick the next move from the new spot
          self.last_spot = state               # remember for the NEXT call's transition
          self.last_move = action
          return action

      # --- the last moment ---
      def agent_end(reward):
          zero_spot = np.zeros_like(self.last_spot)   # dead placeholder: there is no next spot
          # terminal 1: the target of this transition will be the bare reward
          self.buffer.append((self.last_spot, self.last_move, reward, 1, zero_spot))
          replay_learn()                       # one last learn; no move to return

  THE ANCHOR, unchanged since Chapter 13: a move that ended the run has terminal = 1, so its target
  drops the faded-future piece and is the reward alone -- baked into the stored flag, read by the
  batch's (1 - terminal) in Part 3.

  ---

  ONE BREATH

  Parts 1 to 3 built the machine that trains a Q-network -- backprop, Adam, replay, the frozen twin --
  but a pile of functions is not an agent. The world runs the loop and CALLS the agent at three kinds
  of moment: agent_start (first spot, no reward -- pick a move by softmax over the net's worths and
  remember last_spot and last_move), agent_step (every middle moment -- store the 5-tuple
  (last_spot, last_move, reward, terminal 0, new spot), run the replay learn from Parts 1 to 3, pick
  the next move, remember it), and agent_end (run over -- store the same 5-tuple but terminal 1 with a
  dead placeholder spot, learn once more, return nothing). last_move must always hold the move whose
  reward the NEXT call receives, or a reward gets pinned on the wrong move (store (S1,A0,...) when A1
  caused R2); the two "remember" lines guarantee the right blame. The terminal flag 1 makes the
  ended move's target the bare reward via Part 3's (1 - terminal). Strung together over a few hundred
  runs, the three calls are a lander that learns to fly.

  ---

  >> NOTE: STANDARD JARGON
  >> If you read other RL material after this page:
  >>   agent_start / step / end        = the agent's episode interface (start, step, end)
  >>   the world calls the agent        = the environment-agent loop (RL-Glue / Gym style)
  >>   a spot (eight dials)             = a state s (the observation vector)
  >>   pick a move by softmax + sample  = the Boltzmann / softmax behaviour policy
  >>   last_spot / last_move            = the stored previous state and action
  >>   the 5-tuple in the bin           = a transition (s, a, r, terminal, s') in the replay buffer
  >>   the terminal flag                = the done / terminal bit
  >>   terminal 1 -> target is reward   = no bootstrap at episode end (the (1 - terminal) mask)
  >>   the replay learn                 = a minibatch update (Parts 1-3: TD error, backprop, Adam)
  >>   the three calls over many runs   = a full deep-RL control agent (e.g. on LunarLander)

----------------------------------------------------------------------------------------------
  IN THIS CHAPTER (Chapter 15 -- Training the Q-Network):
    Part 1 -- [Backpropagation by Pencil](backprop-by-pencil.txt) .
    Part 2 -- [Adam and Replay by Pencil](adam-and-replay-by-pencil.txt) .
    Part 3 -- [The Frozen Twin: A Batch of Misses at Once](batch-td-error-by-pencil.txt) .
    Part 4 (this post) -- the chapter closes here

  <- [Back to all posts](../index.txt)
----------------------------------------------------------------------------------------------
  (c) 2026 Rahul Rai . pure HTML+CSS, no JavaScript, no trackers .
  [home](../index.txt) . [source on GitHub](https://github.com/learnrahulrai-ui)
==============================================================================================
